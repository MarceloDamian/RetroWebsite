import NavigationThruPages from 'mailgun.js/common/NavigationThruPages';
import { EventsList, EventsQuery, EventsResponse } from 'mailgun.js/interfaces/Events';
import Request from 'mailgun.js/request';
export default class EventClient extends NavigationThruPages<EventsList> {
    request: Request;
    constructor(request: Request);
    protected parseList(response: EventsResponse): EventsList;
    get(domain: string, query?: EventsQuery): Promise<EventsList>;
}
